extends: base.yaml
runtime:
  seed: 7
model:
  hidden_dim: 32
  encoder_layers: 1
  decoder_layers: 1
  attention_heads: 2
  conv_filter_size: 64
  variance_filter_size: 32
  postnet_layers: 3
vocoder:
  channels: 32
training:
  batch_size: 2
  epochs: 1
  num_workers: 0
serving:
  max_concurrency: 1

