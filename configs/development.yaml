extends: base.yaml
runtime:
  device: cpu
model:
  hidden_dim: 128
  encoder_layers: 2
  decoder_layers: 2
  conv_filter_size: 256
vocoder:
  channels: 128
training:
  batch_size: 4
  epochs: 2
  num_workers: 0
serving:
  expose_normalized_text: true

